# NNN - <title>

- **Status**: draft | approved | in-progress | complete | abandoned
- **Date**: YYYY-MM-DD
- **Commits**: <short shas once implemented>

## Context

Why this change - the problem, what prompted it, intended outcome.

## Design

The approach, key interfaces/contracts touched (which `schemas/`), and
alternatives rejected (one line each on why). Note any new cluster permissions
or cost impact.

## Steps

Ordered, each independently verifiable. Mark what can be validated offline
(`bash -n`, schema parse, dry-run) vs. what needs a live cluster.

## Verification

Exact commands/checks proving it works end-to-end. On completion, log evidence
in `.ai/test-results/` and link it here.

## Risks / open questions

Known quirks, guardrails touched, things to verify at implementation time.
